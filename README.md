# OFFSITE.DARK

Security research lab. Red, black, white. Nous-inspired editorial layout with brutalist grid.

## Dev

```bash
npm install
npm run dev
```

## Content

Add markdown to `content/blog/` (research) or `content/news/` (signals).

## Deploy

Vercel or Railway — `npm run build` / `npm start`. No env vars required.
